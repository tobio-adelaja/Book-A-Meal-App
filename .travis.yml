env:
  global:
    - CC_TEST_REPORTER_ID=7b41a0ba8eb70ee1fcc480226e74747da8d200931e72ab25404c3b6dd96ff24b
language: node_js # Sets which engine use to build the project
node_js: "node" # Specifies node version to use "node"="latest"

before_script:
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build

script: 
 - mocha --compilers js:babel-core/register "server/test/menuTest.js" --exit

after_script:
  - ./cc-test-reporter after-build --exit-code $TRAVIS_TEST_RESULT